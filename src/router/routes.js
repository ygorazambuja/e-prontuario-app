import { routes as home } from "../modules/home";
import { routes as login } from "../modules/login";
import { routes as documento } from "../modules/documento";

export default [...home, ...login, ...documento];
