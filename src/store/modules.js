import { store as login } from "../modules/login";
import { store as documento } from "../modules/documento";
import { store as paciente } from "../modules/paciente";

export default { login, documento, paciente };
